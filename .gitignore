# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Node
node_modules/

# Local config & secrets (never commit)
config.json
.env

# Pipeline database (SQLite = events/logs only, disposable)
*.db

# Vite build output
web/dist/

# Stray nested clone (accidental duplicate, never re-add)
web/brain-cockpit/

# Watcher runtime heartbeat (liveness only, disposable)
.watcher-heartbeat

# Manual backups (events.db copies — disposable, vault is the truth)
backups/
